{
  "name": "cpd-monitoring",
  "version": "1.0.0",
  "description": "### You can run that with:",
  "main": "index.js",
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix frontend",
    "dev": "npm run dev-server-and-frontend",
    "dev-server-and-frontend": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^7.0.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
